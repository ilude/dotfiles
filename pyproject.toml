[project]
name = "dotfiles-tests"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "pytest",
    "pyyaml",
    "ruff",
]

[tool.pytest.ini_options]
testpaths = ["test", "claude/hooks/*/tests"]
