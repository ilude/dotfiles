[project]
name = "session-history-hook"
version = "0.1.0"
description = "Claude Code Stop hook for session history capture"
requires-python = ">=3.9"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.0"]
