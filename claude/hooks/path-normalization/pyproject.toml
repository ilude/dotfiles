[project]
name = "path-normalization-tests"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = []

[dependency-groups]
dev = [
    "pytest",
    "pytest-cov",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
