[project]
name = "damage-control-tests"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pyyaml",
    "pytest",
    "pytest-cov",
    "pytest-asyncio",
    "tree-sitter>=0.23.0",
    "tree-sitter-bash>=0.23.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
