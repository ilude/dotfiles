HIGH | Cloudflare token may be exposed through rendered env files and container inspection/logs (`Render the token into infisical.env or a separate Caddy env file`) | Prefer Docker/Ansible secret file injection where possible; if env is required, ensure `no_log: true`, `0600`, gitignored path, no `--diff` secret output, and document `docker inspect` exposure risk.
HIGH | Plan enables public ports by default (`Keep ports 80/443 bound unless...`) for an internal Infisical service | Require an explicit exposure decision: default bind to LAN interface or reverse-proxy internal network only; document firewall rules and Cloudflare/public DNS expectations.
MEDIUM | DNS-01 validation is conflated with local-only DNS success; public authoritative DNS requirements are under-specified | Add a required check that `ilude.com` is active in Cloudflare and Caddy can create `_acme-challenge.infisical.ilude.com` TXT records; local Joyride A record is not sufficient for ACME.
MEDIUM | `DNS_UNKNOWN_ACTION=drop` accepted despite availability risk | Add explicit validation against real client resolver behavior and rollback option (`forward`/alternative unknown handling) before relying on it in the runbook.
MEDIUM | Caddy plugin build is not pinned | Pin Caddy version, xcaddy version, and `caddy-dns/cloudflare` module version/commit; add rebuild/update procedure and vulnerability review gate.
