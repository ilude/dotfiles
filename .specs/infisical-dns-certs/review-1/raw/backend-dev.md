High | Plan requires Cloudflare token but not Cloudflare zone ID; Caddy DNS plugin may need zone lookup/read and failures are hard to diagnose | Add explicit handling/documentation for zone ID or confirm plugin can discover via Zone:Read; include required token permissions: Zone DNS Edit + Zone Zone Read for `ilude.com`.
High | `DNS_UNKNOWN_ACTION=drop` is accepted while Joyride test uses direct `dig @192.168.16.241 -p 54`; direct clients querying unknown names may hang/time out | Require runbook to document Joyride must not be the sole/general resolver unless upstream/client resolver behavior is verified; add validation for both known and unknown DNS behavior.
Medium | Caddyfile always enables DNS-01 when `infisical_caddy_dns01_enabled` exists, but no conditional template behavior is specified | Require `Caddyfile.j2` to conditionally render DNS provider only when enabled and fail fast if enabled without `vault_infisical_cloudflare_api_token`.
Medium | Docker label only sets hostname, but no target IP/container network behavior is verified for Joyride labels | Require validation that Joyride label resolves `infisical.ilude.com` to `192.168.16.241`; otherwise prefer/static-host fallback as default for host-published Caddy.
Medium | Validation commands use `docker compose run --rm ansible ...` without vault password handling, making live deploy instructions incomplete | Add explicit vault password mechanism (`--ask-vault-pass`, vault password file, or documented env/secret mount) without exposing secrets in logs.
