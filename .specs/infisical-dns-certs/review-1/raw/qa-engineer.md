High | T2 says render `vault_infisical_cloudflare_api_token` into `infisical.env`, likely shared with Infisical app container | Use a dedicated Caddy-only env file mounted only into the Caddy service with `0600`, and ensure Ansible tasks use `no_log: true`.
High | Plan requires `DNS_UNKNOWN_ACTION=drop` while local validation only tests known hostname resolution | Add validation for unknown/non-local DNS behavior from an actual client resolver path, or document exact upstream resolver configuration required to avoid user-visible DNS timeouts.
Medium | Caddyfile DNS-01 config lacks explicit ACME email / staging guidance | Add defaults/runbook steps for ACME email and optional Let’s Encrypt staging toggle to avoid rate-limit issues during first deployment.
Medium | Docker image build says pinned Caddy runtime image but does not require pinning `xcaddy`, plugin version, or Caddy version consistency | Require pinned builder/runtime Caddy versions and a pinned `github.com/caddy-dns/cloudflare` module version; validate built image includes the module.
Medium | Validation commands use logs and curl but do not verify issued cert chain/issuer/SAN | Add `openssl s_client` or equivalent certificate inspection to prove trusted LE cert and hostname match.
