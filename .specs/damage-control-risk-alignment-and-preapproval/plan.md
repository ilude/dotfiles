---
created: 2026-09-08
updated: 2026-09-08
status: blocked
completed: null
---

# Risk-based Damage Control and readable policy, followed by script preapproval and failed-call protection

## Goal and scope

Make default Pi Damage Control prevent meaningful unrecoverable harm without treating routine recoverable work or suspicious-looking syntax as inherently dangerous. Implement the operator's broader scope: align all policy families identified by the completed risk review, improve contextual command decisions, add `/dc scan` and single-script preapproval, and retain a narrow unattended failed-call watchdog.

**User requirements:**

- Actual consequences govern. No inherited rule is exempt from reconsideration merely because it existed in legacy. Routine recoverable maintenance should run quietly; credible loss of meaningful data, uncommitted work, recovery mechanisms, important system state, sensitive information, or substantial irreversible resources warrants intervention.
- Use deterministic decisions for established routine cases and Luna judgment for consequential contextual cases. Neither unfamiliar syntax nor missing parser information alone establishes danger. Independent user/project authorization, including push/deployment restrictions, still applies.
- `/dc scan` reviews project-owned scripts using parallel subagents, without executing them. Repeated scans avoid re-reviewing unchanged completed results. Matching preapproved invocations skip script-body analysis; everything not covered falls back to normal runtime analysis.
- Store approvals in the repository's common Git directory, shared by all worktrees. Only outside Git, use the current project/location's `.pi/` directory. Preapproval can cover a script generally or a particular argument form when arguments materially change danger.
- Add `Allow once and review for future use` alongside `Allow once` and `Deny`. Run the permitted call without waiting for review; a qualifying review creates future approval without a second confirmation.
- Keep an unattended watchdog for repeated exact failed tool calls. Successful repetition is not a trigger. The operator proposed a tolerance around 12 failures, motivated by a reported June 2026 loop consuming roughly half a weekly Codex allowance; that incident was not independently verified during planning.

**2026-09-08 operator follow-up:** The shadow judge is expected to recognize ordinary temporary-resource patterns such as `scratch=$(mktemp -d); ...; rm -rf "$scratch"`. The exact generated pathname need not be statically known to judge that lifecycle. Opaque policy names such as `legacy-007` do not communicate what a policy means. Repair routing across the reviewed families and replace migration-order identities, rather than adding another temporary-path parser or patching only two rm rules.

**Delivery ordering:** Stage A is T1-T3 plus T3A: readable policy and working contextual review. Stage B is watchdog and script-preapproval work, T4-T8. The execution invocation authorizes both stages, local commits and integration. Stage A does not depend on D1 or Stage B's review machinery. Neither stage is delivered until its acceptance checks and local integration finish; do not archive partial implementation.

**Non-goals:** legacy changes; a new shell/parser framework, OS sandbox, universal harmlessness proof, package/dependency manager, mandatory project scans or environment inventories, broad self-integrity system, repeated security audits, fuzzy loop detection, durable job service, new production telemetry, extra approval wizard, or changes to Onclave/infrastructure. Do not add rollback work. Reuse the delivered default subagent runtime for Stage B, not a separate launcher or broker. "Shadow judge" here means the existing pre-execution Luna reviewer that can allow a pending call, not a new observational/background evaluator or review-mode framework.

**Authorization:** The operator's execution invocation authorizes the full selected plan, local commits and local integration. D1 remains a conflicting recorded decision requiring confirmation. Execution then includes local task commits, archival and merge to recorded `main`, unless restricted; push and deployment remain separate. The completed risk review is input, not another audit to repeat.

## Context for a fresh session

Repository-root-relative paths below refer to `C:/Users/mglenn/.dotfiles`. In abbreviated task/input lists, **default** means `pi/profiles/default/`, and bare Damage Control source filenames mean `pi/profiles/default/lib/damage-control/`. Default `extensions/`, `docs/`, `tests/` and `scripts/` paths are profile-relative; root `scripts/pi-deps-link-setup`, `pi/README.md`, `.specs/` and `CHANGELOG.md` are repository-relative. Read root/default `AGENTS.md`, `pi/README.md`, planning/testing skills, and `pi/profiles/default/docs/damage-control-{port,setup}.md` and the co-located [risk review](damage-control-risk-review.md). Read only the installed Pi API docs/examples needed for the next task, completely, including relevant cross-references.

- Baseline: originally `15c7aee6`; planning refresh inspected `fa1c2976` on `main`, 2026-09-08. Recheck current source and status before execution; do not reset to a historical commit.
- Resumed execution worktree/branch: `C:/Users/mglenn/.dotfiles/.worktrees/damage-control-risk-alignment`, `feature/damage-control-risk-alignment`. Recorded parent integration target: `C:/Users/mglenn/.dotfiles`, branch `main`, as recorded by the existing task. The worktree was clean at `66a2f416` with prior implementation commits `f1cca879`, `d79153b0`, `66a2f416`; merged current main `fa1c2976` in `9cc9c57`, preserving both changelog entries. This selected plan refresh supersedes stale planning text, not prior implementation/evidence.
- Existing runtime: default `extensions/damage-control/index.js`; `lib/damage-control/{adapters,analysis,shell,paths,engine,enforcement,context,sequence,breaker,policy,types,judge,prompt,approval,approval-view}.ts`; `lib/damage-control/judge-prompt.md`; `damage-control-{rules.yaml,settings.json}`.
- Existing checks: default `tests/damage-control/`, `scripts/damage-control-{eval.ts,smoke.mjs}`; pnpm owns dependencies. YAML and `proper-lockfile` are already installed. Do not add a database or dependency merely for trust storage.
- Carry the task-owned plan refresh and APR-010 feedback correction into the worktree without overwriting their originals. Earlier risk-review/design-context changes may already be committed; check rather than copying their historical dirty state. On this refresh, unrelated changes exist in `CHANGELOG.md`, default Bedrock ledger/tests, and other entries in the failure log. Preserve them and any newly concurrent work.
- The default subagent runtime is now present on `main`; see default `docs/subagents.md` and `.specs/archive/default-subagents-and-council/plan.md`. The earlier unfinished-prototype handoff is superseded. Stage A has no subagent-runtime implementation dependency. Inspect the delivered API only when Stage B's reviewer integration starts; do not import source across worktree paths or reopen subagent implementation.

### Profiles and verified planning evidence

- Planning profile: verified `PI_CODING_AGENT_DIR=C:\Users\mglenn\.dotfiles\pi\profiles\default`. `scripts/pp` defaults to `default` and resolves repository profiles relative to its own checkout.
- Intended execution/validation profile: the task worktree's absolute `pi/profiles/default/`, not a lasting-checkout launcher/profile. Legacy remains unchanged.
- Worktree dependency setup, if missing: `pnpm --dir pi/profiles/default install --frozen-lockfile`, then `bash scripts/pi-deps-link-setup --profile default` from that worktree. Inspect/reuse working dependencies instead of reinstalling routinely. Existing approval policy still governs until changes are implemented.
- Installed Pi 0.85.0 docs read: README, `docs/extensions.md`, `docs/environment-variables.md`, `docs/tui.md`, and `examples/extensions/bash-spawn-hook.ts`. Resolve future installed docs/source through default's linked `@earendil-works/pi-coding-agent`, not a hardcoded store hash.
- Native shell source builds an environment from `getShellEnv()` (process environment with Pi's bin directory on PATH), then injects session variables, then applies an optional spawn hook. Commands may also receive a prefix. Extension `tool_call` happens before tool execution and does not expose that final spawn context. Do not equate a gate-time `process.env` snapshot with all later shell values or a remote Herdr pane's environment.
- `tool_result` exposes `isError`, original input and call ID. Pi preflights sibling calls before executing them concurrently; results can arrive in completion order. `terminate: true` stops a batch only when every finalized result terminates; it is not by itself a reliable mixed-batch watchdog stop. `ctx.abort()` is available. No prototype for these new integration paths has been run.
- Current parser starts with an empty variable scope; normal Luna review has no tools. Current `engine.ts` gives confirmed user/block matches precedence over review. `enforcement.ts` calls Luna only for `outcome: review`, and `judge-prompt.md` expressly forbids allowing a confirmed user rule. Generic rm rules `legacy-007` and `legacy-008` are user-only, so the reported pattern cannot be cleared by Luna under this source. No trace of the reported invocation was captured; this is source-level routing evidence, not an observed judge response.
- `policy.ts` currently permits lowercase kebab-case IDs and rejects unknown fields. The earlier chat example using dotted IDs and `source:` is not valid current configuration. Prefer semantic kebab-case IDs and keep migration provenance outside runtime authority; no schema expansion is required solely for naming.
- `paths.ts` also generates ordinal IDs (`path-<category>-<index>`). `analysis.ts` removes user/review matches for deletion contained below cwd, without proving recoverability. Sequence correlation injects confirmed user matches in `enforcement.ts`. All three participate in effective routing, not just the YAML action fields.
- Current breaker is tied to covered Damage Control adapters and identical result text, and also limits successful calls. Its changes remain Stage B.

| Date | Actual profile | Work/check | Result and limits |
| --- | --- | --- | --- |
| 2026-09-08 | Lasting default | Risk review: 18 parser/engine cases plus one sequence case | Real analysis with synthetic Windows path facts and fixed parser clock. No submitted commands or model calls executed. Full findings in the co-located `damage-control-risk-review.md`. |
| 2026-09-08 | Lasting default | Original planning/source/docs inspection | Confirmed profile, existing dependencies, shell spawn ordering, event semantics and then-unfinished subagent dependency. No product implementation or new runtime acceptance. |
| 2026-09-08 | Lasting default | Plan refresh at `fa1c2976` | Confirmed policy routing, numeric/positional identity seams, eval limitations and delivered subagents; corrected stage boundaries. No new runtime/model checks. |
| 2026-09-08 | Worktree default | Baseline reproduction | Exact original Herdr command recovered as inert data. Real `main` analysis at `fa1c2976` returned `user`, confirmed `legacy-007`/`legacy-008`. No submitted command executed. |
| 2026-09-08 | `C:/Users/mglenn/.dotfiles/.worktrees/damage-control-risk-alignment/pi/profiles/default` | Final offline checks, with `PI_CODING_AGENT_DIR` explicitly set to this profile | 188 tests in 18 Damage Control files passed. `check:runtime` passed policy/grammars/native schemas/bundled loader using its documented empty temporary child profile. `typecheck` reports only pre-existing TS7016 in unchanged `tests/commit-whitespace.test.ts:5` for `commands/commit/trim-trailing-whitespace.mjs`. No declaration workaround or unrelated command edit added. |
| 2026-09-08 | Worktree default, evaluator paths resolved from its own module | Production-route synthetic evaluation | All specified initial routes now match: original Herdr and minimal mktemp reach review; protected and encoded-home contrasts block. 25 fixtures: five deterministic outcomes passed, 20 model judgments unverified, zero provider calls. Both task-profile `auth.json` and `models-store.json` are empty objects; no `models.json` exists. No credentials were copied or alternate model/profile substituted. |
| 2026-09-08 | Windows / WSL Ubuntu | Platform availability | Windows checks above completed. Ubuntu has no native Node executable; its visible pnpm is the Windows shim. Native Linux TS checks remain unavailable without new platform setup, which this plan excludes. |

## Decisions and contracts

### 1. Policy outcomes across the full reviewed scope

Use the existing allow/review/user/block structure. Do not replace it with a general permissions framework. A rule match identifies an operation; its actual risk determines authority. Generic operation detectors may remain while mechanics-only mandatory restrictions are removed. Do not let an overlapping generic user/block rule defeat a deliberate contextual disposition.

### Stage A routing and identity contract (proposed implementation)

- **Allow:** established ordinary work needs neither Luna nor UI. Do not add review to every command.
- **Review:** an operation whose consequences depend on context reaches Luna once with the complete pending call and relevant existing evidence. Generic recursive/forced deletion belongs here, not in mandatory-user policy merely because flags match. Luna can recognize an ordinary `mktemp` lifecycle without a parser-produced concrete pathname. A variable named `tmp` alone is not that evidence.
- **User:** reserve direct mandatory approval for an explicitly justified human-only boundary. Each retained user rule must identify that boundary and why contextual allowance is insufficient. No numeric grandfathering. A possible harmful operation is not automatically a mandatory-human rule when Luna can distinguish harmless instances.
- **Block:** retain narrowly defined consequential prohibitions such as root/home destruction and established forbidden sensitive disclosure. Review all existing blocks too: an inherited syntax/encoding/sudo block must not silently veto an otherwise reviewable operation. A confirmed retained block still wins over an allow result.
- Review failure/timeout/invalid output on a reviewable operation still escalates; cancelled or changed calls cannot execute. Keep at most one judge call per pending call, no approval cache, and preserve whole-call accounting. Do not relabel confirmed operations as regex false positives just to let Luna dismiss them.
- **Identity:** replace all active migration-order command IDs with stable semantic kebab-case names, for example `filesystem-rm-recursive-or-force` and `filesystem-delete-home-or-root`. Names identify behavior, not current action or list position. Consolidate demonstrated duplicates only when matching coverage is preserved; semantic naming does not require a regex rewrite.
- Path-generated matches must identify the protection and relevant pattern instead of only an ordinal. Sequence and runtime-generated matches must be equally understandable in evidence and Details. Use a small existing-schema-compatible representation where practical, not a new policy language.
- Keep `tests/damage-control/fixtures/policy-migration.json` as historical evidence. Record old-to-new, merged, and removed dispositions in a compact task-local table (proposed `policy-dispositions.md` beside this plan). Historical IDs may remain there and in archives, not as active authority or numeric ranges in the judge prompt. Do not add `source:` to every rule merely to carry provenance into the runtime.
- Update the judge contract to express consequence-based authority across the reviewed families, rather than a whitelist of legacy numeric ranges or local-only exceptions. Parser uncertainty is evidence to interpret, not an automatic approval request.

Stage A applies the R1-R7 dispositions below. R8/R9 belong to Stage B. No fresh audit is required: use the completed review to account for retained, changed, merged, and removed policies.

This table is the target family-level contract, not a requirement for a separate approval dialog or test for every one of the 335 legacy entries. Update the existing risk-review table with concrete retained/removed/changed rule IDs as implementation evidence.

| Family / review finding | Required outcome | Representative acceptance |
| --- | --- | --- |
| Routine cleanup and local tooling, R1 | Established rebuildable caches and empty-directory removal allow without Luna/UI. Mixed-risk environments, images, volumes and package purges use consequence-based judgment, not a blanket safe label. | `uv cache clean`, `go clean -cache`, empty-directory `rmdir` allow; meaningful volume deletion does not inherit that exemption. |
| Flags, encoding, scheduling and permissions, R2 | Remove prohibition based solely on syntax, encoding, history clearing, scheduling/persistence vocabulary, or privilege. Analyze supported decoded operations; review consequential uncertainty. Preserve actual destructive target protection. | Harmless encoded output and Bash/PowerShell disposable cleanup agree; corresponding real destructive operation still intervenes. No new dialect required. |
| Files and sensitive paths, R3 | Remove hygiene-only blocks on ordinary lockfiles, generated artifacts and recoverable project documents. Narrow sensitive-name/exclusion rules to actual loss/disclosure concerns, with existing context where sufficient. Do not blindly allow all credential-looking files or inspect every file's contents. | Ordinary lockfile update/public certificate use can pass; known private-key disclosure cannot pass solely through an exclusion name. Synthetic credentials only in tests. |
| Deletion of existing work, R4 | Containment alone no longer means disposable. Available evidence of disposable/recoverable work supports allowance; credible uncommitted/unique data loss prompts. No per-delete recursive inventory, Git subprocess or creation ledger requirement. | Known scratch cleanup passes; supplied evidence of unique uncommitted work prompts even below cwd; root/home destruction stays blocked. |
| Variables and contextual authority, R5/R7 | Resolve relevant static/inherited values when supported by the execution boundary. Pass relevant facts to Luna for reviewable cleanup. Unknown names alone do not prompt; consequential unresolved target scope may. | Same established disposable target spelled literally, via assignment or supported inherited variable has equivalent outcomes. Values from unrelated shell environments are not asserted as fact. |
| Sequence correlation, R6 | Actual disclosure stays protected. Recent sensitive access followed by an unrelated upload is reviewable evidence, not conclusive user-only/block authority. Reset sequence evidence with its owning session. | Synthetic `.env` read then unrelated public upload can be cleared; actual sensitive-source upload cannot. |
| Git, cloud, infrastructure, hosting and publication, R7 | Apply the same philosophy to all remaining reviewed families. Preserve meaningful data/recovery loss and irreversible disclosure/cost protection. Reversible metadata or established disposable resources need not prompt merely because remote. Independent deployment/push authority stays intact. | Recoverable ref/metadata or disposable-resource case can pass with sufficient context; uncommitted-work reset, unique backup deletion and unintended sensitive publication still intervene. |
| Watchdog, R8 | Count exact failed calls, not repeated success, elapsed polling time or model productivity. | Contract below; no polling allowlist remains necessary. |
| Script reuse, R9 | Matching approved invocations skip body parsing and model review; surrounding operations are still analyzed. | Preapproved setup does not rescan its body; appended consequential deletion is separately reviewed/prompted. |

Keep user approval for intentionally requested consequential work where appropriate, and direct blocks for clear destructive prohibitions such as root/home wipe and established forbidden disclosure. Do not classify all recoverable operations as requiring Luna, or all possibly destructive operations as categorically prohibited. User-only and block reasons must identify consequences, not just restate a command flag. Current exemptions for direct operator shell and `/commit` internals remain; this is not a separate review of those product boundaries.

### 2. Script identity, storage and runtime matching

Proposed implementation uses small typed records in YAML, indexed in memory. A record identifies a repository-relative script, its byte-content SHA-256, review outcome/reason, and either whole-script approval or exact approved argv. Add cwd or environment equality conditions only when review identifies their material effect on risk; no condition language, executable predicates or broad regex grants. Completed nonqualifying review results may be retained by hash to avoid redundant `/dc scan` model calls, but never bypass runtime analysis.

- Discover the repository from the operation's cwd, not the profile installation directory. Resolve the common Git directory using Git metadata; linked worktrees share `<git-common-dir>/pi/damage-control-trust.yaml`. Submodules remain separate repositories. Outside Git, use `<ctx.cwd>/.pi/damage-control-trust.yaml`; do not invent an ancestor project-detection service.
- Scan/match the actual worktree's file against the shared record. On a hash/condition miss, missing record, or unreadable/malformed store, use ordinary analysis, not a new approval requirement or global failure. Never auto-update a hash to preserve approval.
- Runtime still reads/hashes covered bytes; it skips body parsing/effect extraction and model review. Cache the YAML index and notice other instances' writes. Use the existing file-lock dependency plus atomic read-modify-write for shared updates. No database, background file crawler or hashing daemon.
- Protect the approval file as operator-authorized configuration using the existing narrow confirmation mechanism. Damage Control writes results of explicitly commissioned reviews; neither script text nor a reviewer's ordinary file-write tool can self-authorize an entry. Do not expand this into protecting all implementation files or a new trust wizard.
- Keep helpers proportional: proposed mechanism is a flat set of additional file hashes only where the reviewed invocation's consequential behavior depends on local helpers or a command definition. A changed required input invalidates that record. No dependency resolver, bundler, lockfile for installed packages, or automatic rejection solely because helpers exist. Unresolved helpers matter when they prevent a credible low-risk conclusion. Document ambient interpreter/package/startup assumptions rather than claiming an immutable execution environment.
- For an invocation that matches preapproval, skip that body, including its internal pattern checks. Do not claim internal hard blocks are reevaluated without parsing. Outer script invocation arguments, redirections, substitutions and additional commands still have their normal effect/authority checks. Keep source provenance so one script's grant cannot erase an adjacent command's findings.

### 3. Review and operator flow

Use one Damage Control review function for both entry points, adapted to the delivered subagent runtime. Its input identifies the script snapshot, invocation/conditions, originating repository/worktree/session, and relevant risk context. Its result is preapprove-with-reason/conditions, retain-runtime-analysis-with-reason, or failed/cancelled. Only a completed qualifying review can create approval, and only for the bytes actually reviewed. Source changes during review leave the new version unapproved.

`/dc scan` inventories project-owned scripts, excludes ignored dependencies/generated output, and partitions work among a small set of parallel read-only reviewers. Use current tracked and nonignored files for Git projects; ordinary bounded traversal for non-Git directories. Include existing supported script languages and extensionless shebang scripts. Package command definitions participate when they are a concrete entrypoint/helper, not through analysis of the complete dependency ecosystem. No arbitrary file-count completion requirement, automatic recurring scan, review consensus, or separate task registry.

Reviewers examine effects and recoverability, not a universal checklist. They never execute submitted scripts or gain shell/write/delegation tools merely to review. Use the new runtime's existing role/permission/surface/lifecycle behavior. The coordinator validates results and writes approved records; progress and completion remain concise, with script-specific reasons available when not approved. Valid completed results may be saved if another batch fails or is cancelled. Reuse the runtime's cancellation/inspection instead of adding a new job dashboard.

The prompt option is offered for an identifiable local script, not for arbitrary direct `rm` patterns. Preserve `Allow once` as the initial selection, Details, Escape-deny, RPC and no-UI behavior. Selecting review approves the whole pending call once, but future approval covers only the reviewed invocation. Dispatch asynchronously without waiting for review or scheduling a future prompt. If review is unavailable/fails, the current permission remains Allow once and future calls use normal analysis. No automatic model fallback or retry loop. Capture the originating context so later completion cannot write into a newly active project's store or notify the wrong chat.

### 4. Failed-call watchdog

The following is the recommended concrete contract, with one operator choice still open:

- Key: tool name + stable arguments + effective cwd. Preserve command text; do not use fuzzy similarity. Track Pi-reported failures (`isError`/thrown execution failures), including changed error output for the same call. Do not infer failure from words in successful output.
- Observe all model-callable tool events, including tools without a Damage Control policy adapter, without inventing policy adapters for them. Count each call ID once. Native shell nonzero exit/timeout uses the tool's real failure signal; verify this through the actual hook path.
- Threshold: allow at most 12 failed attempts, then refuse the next equivalent attempt and halt the affected agent run with the tool, command/input summary, cwd and count. No unattended confirmation loop or automatic resume. Do not terminate unrelated servers or Pi instances. Apply in child instances through their normal Damage Control loading; report failure to their owner through the existing runtime rather than adding cross-process counters.
- No limits on repeated successes. The same call succeeding clears its failure streak. Counts are session-owned, not persistent global history; ordinary assistant turns or compaction must not reset an ongoing loop. A tripped run stays stopped until a new direct operator instruction resumes it; queued automatic follow-ups must not immediately undo the stop. Verify reload/session lifecycle using the installed API, without a durable watchdog service.
- **D1, unresolved:** Do unrelated calls reset the streak? Recommend **no**, so repeated `failing command -> read log -> failing command` is covered. Reset the same signature on success or direct operator resumption; keep different calls independent. Alternative: only adjacent identical failures count, which is simpler but misses that interleaved loop. The operator asked for the plan without explicitly choosing between these interpretations. Settle D1 before T4; do not silently treat this recommendation as an accepted requirement.

## Execution guidance

Create/resume the task worktree when assigned, carry task-owned dirty context safely, then implement and validate there. Record the authorized stage before starting. Preserve shared-file edits and do not modify another task's worktree. Stage A uses current default Damage Control without a subagent integration project. Stage B consumes the delivered subagent runtime rather than creating another implementation.

When assumptions fail, choose a simpler mechanism within these requirements and record the change by its task. Ask only when scope, authority, or acceptance would materially change. Before adding machinery, identify the requirement and observed evidence that needs it. If drift appears, stop the detour, remove only task-created extras while preserving required behavior, and resume the next unmet dependency. Scope checkpoints are not approval gates. No extra broad audit, repeated installation or growing verification corpus.

## Tasks

- [x] **T1 - Establish the execution seams and concrete dispositions.** Depends on execution authorization; no dependency on subagents being finished.
  - Inputs: current policy/risk-review table, `policy.ts`, `paths.ts`, `analysis.ts`, `sequence.ts`, `engine.ts`, `enforcement.ts`, `adapters.ts`, and installed native shell/tool event source.
  - Do: establish/resume the task worktree; carry this plan/design context. First capture the reported Herdr command as an inert regression input, with its intended isolated-test context. Record initial decision, matching policies, and whether Luna/UI were reached; never execute the submitted command. Create the compact disposition table from the completed review: semantic ID, prior ID(s), operation/consequence, target allow/review/user/block disposition, reason for any retained direct restriction, and representative test. Include command, path, sequence, and containment-exemption seams. Use one harmless synthetic inherited-variable probe to choose the minimal execution-appropriate evidence hook for R5; do not execute substitutions to discover their output.
  - Verify: real analysis/engine on the inert reproduction plus source/API inspection and the single harmless variable probe. Existing dependencies suffice. No live Herdr server or destructive operation is needed. If native environment parity needs a wrapper, use a supported boundary rather than rewriting process execution.
  - Done when T2/T3 have concrete semantic identities, routing dispositions, and evidence inputs. A real authorization conflict gets a focused question with a recommendation; routine naming stays implementation judgment. Helper hashes and subagent API investigation wait for Stage B. Evidence: resumed the clean existing task, recorded its parent target, merged `main`, and preserved prior commits. Exact inert reproduction confirms the original user-tier short circuit. `policy-dispositions.md` accounts for all 335 baseline rules by regex, not position. Reused the prior harmless inherited-variable probe rather than rerunning it. Chosen boundary: explicit execution snapshots may resolve variables; normal gate-process values are bounded contextual observations only, never asserted as the final spawn environment.

- [x] **T2 - Align all reviewed command/path families.** Depends on T1.
  - Inputs: `damage-control-rules.yaml`, `lib/damage-control/{policy,paths,analysis,engine,shell}.ts`, and existing policy/path/shell/enforcement tests.
  - Do: implement the Stage A family/identity table, remove mechanics/hygiene-only restrictions, retain actual consequential protection, reconcile contradictory overlaps and sensitive exclusions, and replace cwd-only disposal classification. Rename active policies and their source/test/diagnostic consumers together; preserve history in the disposition table, not runtime names. Remove migration-equality tests as current-policy authority and replace them with explicit intended routing/protection assertions; do not rewrite historical migration evidence. Keep supported dry-run/inert-data behavior. Use existing facts/context and targeted checks where needed, not blanket inventories.
  - Verify: focused `pnpm test tests/damage-control/policy.test.ts tests/damage-control/paths.test.ts tests/damage-control/shell.test.ts tests/damage-control/enforcement.test.ts` from worktree default after the batch settles. Add a paired recoverable/consequential case for each changed family, grouped where they exercise the same decision path.
  - Done when every R1-R7 family above has a concrete disposition and relevant tests prove both quiet ordinary work and retained damage intervention. Evidence: 321 active semantic detectors, 335-row historical disposition table, narrowed path policy, human-only store/publication/shared-history boundaries and consequence-based review. Removed reason-text and numeric-ID engine exceptions and blanket runtime user-to-review conversion. Cache/history, hygiene-only document/TMPDIR and encoded-wrapper rules no longer block routine work. Focused checks and final 188-test suite passed.

- [x] **T3 - Make contextual judgment use relevant target evidence.** Depends on T1/T2.
  - Inputs: `types.ts`, `shell.ts`, `analysis.ts`, `context.ts`, `sequence.ts`, `engine.ts`, `judge.ts`, `judge-prompt.md`, `enforcement.ts`; proposed new `environment.ts` only if the boundary needs its own module.
  - Do: supply the execution-appropriate variable evidence chosen in T1, with provenance and outbound redaction. Route contextual deletion and mixed-risk remaining families through Luna instead of blanket user precedence. Keep deterministic routine allowance. Turn unrelated sequence correlation into review evidence and correct session reset. Preserve actual sensitive-source intervention. Unknown parser information alone stays nonrestrictive.
  - Verify: extend existing environment/evidence/engine/judge tests and add focused sequence tests. Prove literal/static/inherited target parity, no environment dump, unrelated-upload clearance, actual-disclosure intervention, and at most one normal Luna call per reviewable tool call. No unbounded shell evaluation to discover values.
  - Done when available context can clear routine operations and missing consequential facts produce an honest decision rather than speculative certainty. Evidence: confirmed review operations can be allowed without pretending they are false positives; full command and semantic/path/sequence evidence reach the existing judge. Inherited execution snapshots remain an explicit dependency; bounded gate-process observations carry override warnings and never resolve targets. Credential-like observed values are masked, and outbound projection remains enforced. Real gate tests cover one review/no UI on allow, protected mixed calls, ask/failure/cancel/stale behavior, and related/unrelated disclosure. Live model judgment remains T3A/T7, not claimed by these mocked verdicts.

Scope checkpoint: broader policy alignment is required, not a new security framework. Remove task-created inventories or duplicate classifiers before continuing; do not reopen the audit.

- [ ] **T3A - Independently validate and deliver policy repair.** Depends on T2/T3, not T4-T6 or D1.
  - Inputs: actual production policy, real `analyzeRequest`/engine/gate, existing enforcement/engine/judge/path tests, existing `scripts/damage-control-eval.ts`, default `docs/damage-control-{port,setup}.md`, `pi/README.md`, root `CHANGELOG.md`.
  - Do: add a finite routing/decision matrix using the existing test infrastructure. Include the full reported Herdr command unchanged as data, a minimal same-call mktemp cleanup, a reassigned variable pointing at meaningful data, an ordinary contained path with supplied evidence of unique uncommitted work, a mixed call with temporary cleanup plus a protected deletion, and existing paired scenarios for each changed family. Account for `$PID` being unassigned in the original text without making its exact value a new risk requirement. Unknown path fragments should not replace the complete command in evidence.
  - Verify offline: use real YAML, parser, path analysis, and engine through the gate; mock only Luna/UI/external unsafe dependencies. Assert review invoked exactly once and no UI after allow, review asks/failures prompt, retained confirmed block invokes neither judge nor approval, cancellation/stale calls do not execute, and no-review ordinary work remains quiet. Assert semantic IDs reach judge evidence and Details, and no active policy or judge contract relies on legacy numeric IDs. Preserve historical fixtures. Run `pnpm test tests/damage-control`, `pnpm run typecheck`, and `pnpm run check:runtime` from the task worktree default.
  - Verify actual judgment: extend the existing `pnpm run eval:damage-control --environment` path to use real full `analyzeRequest`/engine processing with synthetic path facts, not hand-injected review rules or shell-only analysis. Add the exact Herdr reproduction and minimal mktemp case plus meaningful-data/mixed-call contrasts; reuse the R1-R7 family pairs rather than making a second corpus. Report per-case initial route, whether Luna was called, final outcome, and a safe reason using existing console output, not a new telemetry store. Review only: never execute these inputs. Expected: both ordinary tmp lifecycles reach the real configured Luna and allow without UI; consequential/forbidden contrasts intervene. Update old eval fixtures which currently assert syntax-only blocks or blanket mandatory volume approval. Provider unavailability is unverified acceptance, not permission to substitute a model or claim mocked judgment as real.
  - Done when the fixed policy is understandable and all finite routing/judgment checks pass. Update operator docs/changelog with the actual behavior, explicit direct-approval/block boundaries, real profile results and limits. Record Stage A delivered with its commit after local integration to `main`, preserving unrelated changes. If only Stage A is authorized, commit the active plan with checked tasks and retain Stage B pending; do not archive or silently start it. If integration or live validation is blocked, record that concrete state rather than calling Stage A delivered. Evidence: offline acceptance and docs completed; exact original input is `tests/damage-control/fixtures/reported-herdr-command.txt`. Full production evaluation now exposes route/judge/final status and has the required contrasts. Blocked by empty task-profile auth/model catalog and unrelated full-profile TS7016. Stage A has not been merged or declared delivered.

- [ ] **T4 - Replace the repeated-success breaker with the failed-call watchdog.** Depends on T1 and D1 resolution; can run independently of T2/T3.
  - Inputs: `breaker.ts`, `enforcement.ts`, extension registration, `breaker.test.ts` and gate fixtures. Add a small owner module only if needed to observe all tool events outside policy adaptation.
  - Do: implement the watchdog contract, remove success/polling restrictions and identical-output dependence, preserve effective cwd/call identity, and stop the run rather than feed another retry loop. Keep failure state separate from sequence/context clearing. Handle actual Pi parallel preflight/result ordering without inventing a global scheduler.
  - Verify: synthetic throwing tool/native nonzero-exit fixtures through real hooks; 12 failures then prevented attempt 13, changed error text, repeated successes, same-call recovery, D1 interleaving, and stop/reset lifecycle including queued continuation. Demonstrate that mixed batches cannot silently restart the stopped loop. Do not consume a real model subscription to simulate 12 failures.
  - Done when the run halts unattended and ordinary successful polling is unrestricted. Evidence: prior commits implement adjacent-failure counting and passing basic breaker tests. Selected draft and prior committed plan conflict on D1; operator confirmation was requested during this execution. Preserved existing behavior rather than silently choosing. T4 remains incomplete: its halt/reset/queued-continuation/reload acceptance still needs completion after D1, and the inherited abort path currently resets breaker state. Do not mistake the basic passing counter tests for the full watchdog lifecycle contract.

- [x] **T5 - Add source identity and a shared preapproval store.** Depends on T1/T2; no live subagent dependency.
  - Inputs: `types.ts`, `shell.ts`, `analysis.ts`, `approval.ts`, existing YAML/locking dependencies. Proposed new `lib/damage-control/{script-trust,script-review}.ts` and focused tests, split only by actual responsibility.
  - Do: record nested script source identity/ranges and immutable review inputs; implement project/common-Git-dir discovery, minimal YAML records, in-memory indexing, coordinated atomic writes, conditional matching and stale invalidation. At T5 start, settle the flat consequential-helper hash representation described in contract 2 using the actual review boundary, then implement it. Integrate the body-skip decision before nested analysis, not as a post-analysis suppression. Normal fallback must work without a store.
  - Verify: disposable Git repo plus linked worktree share approvals but check each file's bytes; non-Git cwd uses only `.pi`; changed script/required helper/argument condition misses; parallel writers preserve both updates; malformed store falls back. Instrument existing analysis calls to prove a match performs zero body parses/model calls while appended operations still get checked. No wall-clock speed threshold or benchmark suite.
  - Done when hash-bound reuse and runtime fallback work offline and ordinary tools cannot silently grant approval by editing the store. Evidence: corrected worktree-relative identity independently of common-dir storage; a real disposable Git repo with two linked worktrees verifies shared approval and changed-byte misses. Runtime hashes recorded helpers without parsing approved bodies. Stat-invalidated store caching, concurrent writes, malformed fallback, snapshot/argv matching and explicit trust-store write confirmation are implemented. Focused trust/review tests and the final suite passed.

- [x] **T6 - Implement `/dc scan` and conditional background review.** Depends on T3/T5 and delivered subagent integration.
  - Inputs: default `extensions/damage-control/index.js`, proposed review/store modules, existing `prompt.ts`, `approval-view.ts`, `approval.ts`, plus the delivered default subagent launch/result interface. Add a narrowly read-only review definition if the existing role cannot meet the needed tool ceiling; do not modify the generic role's authority for everyone.
  - Do: inspect the delivered subagent API, then wire both entry points to one reviewer/coordinator, bounded parallel discovery/review, source-bound result validation and short notifications. Preserve existing `/dc` controls and default selection/Details/Escape behavior. Allow once returns before review completion. Unavailable reviewers leave runtime scanning available; no new operator gate. Use the runtime's origin/lifecycle/cancellation and actual visible/headless defaults.
  - Verify: deterministic reviewer fixtures exercise complete/nonqualifying/failed/cancelled results, source mutation during review, incremental scans, interleaved store updates and TUI/RPC choices. Show current execution is not delayed by review and no approval is written on reviewer failure. No-UI runtime calls still report genuine approval needs normally.
  - Done when `/dc scan` and the prompt option work through actual extension registration and the same review implementation, without executing scanned scripts or building another delegation engine. Evidence: existing extension registration consumes one coordinator and the delivered runtime. Reviewer source/request snapshots are frozen; read-only tool authority is narrowed without modifying the generic role. Scan and invocation cancellation propagate, and late notifications cannot target a replacement session. Unchanged completed results reuse and current execution does not wait for future review. Focused gate/UI/coordinator tests and bundled-loader smoke passed. The live two-child run belongs to T7 and remains blocked by task-profile model/auth availability.

- [ ] **T7 - Verify real integration and reconcile operator documentation.** Depends on T2-T6.
  - Inputs: real bundled Pi loader, delivered subagents, default Damage Control eval/smoke, finite checks below, and the existing risk review/design docs.
  - Do: run the finite integration set; update default `docs/damage-control-{port,setup}.md`, the co-located `damage-control-risk-review.md`, `pi/README.md`, and root `CHANGELOG.md` to distinguish implemented behavior from historical results. Remove superseded blanket-rule claims, successful-poll limits and no-cache statements only where actually changed. Explain hash reading versus skipped parsing and the reviewed helper/environment boundary without promising a sandbox.
  - Done when the agreed checks pass and documentation matches the results. If verification is blocked, keep T7 incomplete, report the concrete blocker and continue independent work; do not archive on mocked evidence alone. No blanket new testing phase after this one. Evidence: operator docs reconciled and offline validation recorded above. Live Luna and two-child scan remain unverified; full-profile typecheck has the unrelated existing TS7016. D1/T4 remain pending. Do not rerun passing checks until relevant code or model configuration changes.

- [ ] **T8 - Archive and integrate.** Depends on T7.
  - Set completion date/status, record actual profile/results, archive this whole spec directory in the task branch, and commit implementation plus archived plan before merging to `main`. Reconcile task-owned original dirty context without absorbing the concurrent subagent incident or unrelated work. Do not overwrite an existing archive.
  - Verify the target contains implementation/archive and no active copy remains. If conflict resolution changes behavior, run only affected checks. Remove the task worktree only after clean integration. No push, production relinking, or submodule changes.
  - Done when delivered locally to `main`; otherwise retain worktree and report implementation versus integration separately. Evidence: not started.

## Finite validation and finish

Stage A finishes at T3A with its stated checks, documentation, and integration. The list below is the whole-plan finish for Stage B. Reuse completed Stage A evidence, repeating policy/judge checks only when subsequent relevant changes make it stale. From the task worktree default profile after integration changes settle:

1. `pnpm test tests/damage-control` for the changed policy, evidence, watchdog, trust-store and UI boundaries. Run only affected new/changed subagent integration filters as well, not a wholesale unrelated legacy/default suite.
2. `pnpm run typecheck` and `pnpm run check:runtime`. Runtime readiness remains the existing bundled-loader smoke, extended only for changed registration/load boundaries.
3. Extend the existing `pnpm run eval:damage-control --environment` corpus once with six paired synthetic scenarios: disposable versus unique-work deletion; resolved safe versus consequentially unknown variable; harmless versus destructive encoded operation; unrelated versus actual sensitive upload; disposable versus meaningful volume/resource data; recoverable versus consequential remote/Git/publication operation. Review only, never execute submitted operations. This verifies sampled judgment, not exhaustive model safety. Reuse existing equivalent cases rather than duplicate them.
4. One isolated bundled-Pi `/dc scan` integration over a disposable repository with a few known harmless, argument-sensitive and nonqualifying scripts using two real read-only children. Verify persistence, unchanged rescan reuse, worktree reuse and changed-version fallback. Exercise the prompt review choice with an inert intercepted operation so no dangerous request is manufactured. Use the delivered runtime's normal host behavior; no separate Herdr capacity/keyboard pilot or production plugin relinking.
5. At the task root, `git diff --check` and changed-document/link checks. Record Windows checks and run affected Bash/path/trust/watchdog offline filters on available native Linux tooling without new platform setup; disclose unavailable platform coverage.

Focused task checks may run as implementation settles. Repeat checks only after relevant changes/failures. Fix demonstrated relevant problems; do not add another corpus, test framework, security review or installation proof after this set passes. Live provider/runtime unavailability is an explicit verification blocker, not permission to change models or claim the mock proved live behavior.

## Current handoff

- Status: blocked, unfinished and not archived. T1-T3 and T5-T6 implementation/offline checks are complete. T3A/T7 live acceptance and T4 watchdog completion are not.
- Worktree: `C:/Users/mglenn/.dotfiles/.worktrees/damage-control-risk-alignment`; branch `feature/damage-control-risk-alignment`; recorded parent `C:/Users/mglenn/.dotfiles`, branch `main`. Local checkpoints are authorized; no push or deployment. Original checkout unrelated dirty work, including Bedrock, subagent-plan and feedback entries, remains untouched. Its task-owned plan now points here for current execution evidence.
- D1 conflict: committed worktree plan `66a2f416` says operator chose adjacent-only failures; the selected draft says unresolved. Asked whether to retain recorded adjacent-only behavior. Do not silently choose either contract. After confirmation, finish T4's explicit stop/reset/lifecycle checks rather than assuming prior counter tests covered them.
- Live blocker: worktree-profile auth/catalog are empty, so the configured `openai-codex/gpt-5.6-luna` is unavailable. Authenticate/configure that profile or obtain explicit permission for an existing authentication/catalog source without substituting task code or model; then run the finite live eval and two-child scan. No reviewed command may execute.
- Typecheck blocker: unchanged `tests/commit-whitespace.test.ts:5` lacks a declaration for `commands/commit/trim-trailing-whitespace.mjs` (TS7016), inherited from merged `main`. No unrelated command files were changed to hide this failure.
- Latest checks: 188 tests, runtime smoke, `git diff --check`, and local Markdown targets passed with explicit task-profile environment for runtime checks. 25 synthetic production-route cases have the expected initial routes, but 20 Luna outcomes remain unverified and zero model calls occurred. Linux unavailable without setup. Do not repeat unchanged passing checks; model configuration/T4 changes justify only their affected checks.
- Next: resolve D1 and model availability, finish T4 and the remaining finite acceptance, then archive the whole spec and integrate to the recorded parent. Until then retain this active plan and worktree; no archive or merge attempted.

## Completion and archive

When T1-T8 and the agreed checks finish, set `status: completed` and `completed: YYYY-MM-DD`, move this entire directory, including `plan.md` and `damage-control-risk-review.md`, to `.specs/archive/damage-control-risk-alignment-and-preapproval/` in the task branch. The review is task evidence, not a permanent runtime doc; archive it with the plan. Repair inbound links and the review's repository-document/archive links for the new depth, then integrate as T8 specifies. Keep unfinished work active. Do not claim writing or approving the plan completes its implementation.
