---
created: 2026-08-25
status: ready
---

# Align agent guidance with simple, flexible enforcement

## Objective

Revise Pi's development guidance and routing policy so consequential invariants remain enforced, contextual choices remain flexible, and advisory or experimental routing machinery has an explicit evidence-backed retain, simplify, remove, or evidence-insufficient disposition instead of persisting as unexamined policy.

## Completion Evidence

- Evidence: Repository guidance consistently distinguishes values, safety boundaries, outcomes, necessary procedures, overridable defaults, heuristics, and contextual judgment; broad determinism and fallback prohibitions are replaced by simpler invariant-focused language; duplicated reasoning and routing choreography is removed or relocated to its owner; safety-critical Git, credential, target, protocol, rollout, and incident controls remain normative; and a reproducible bounded audit records observed routing use, cost, duration, structural validation coverage, external interfaces, and evidence limits before assigning each advisory and experiment mechanism a retain, simplify, remove, or evidence-insufficient disposition, with focused tests passing for every changed executable contract.
- Fails when: A heuristic is still presented as an invariant, flexibility is used to weaken a safety boundary, global instructions still prescribe mutable model ladders or unnecessary reasoning rituals, routing code is deleted without checking active interfaces and available evidence, an experiment remains without a decision or retirement disposition, telemetry correlations are presented as causal quality evidence, or changed Pi routing and instruction-discovery behavior fails its focused validation.

## Boundaries

- In scope: `AGENTS.md`; `pi/AGENTS.md`; the development-philosophy, skills-engineer, orchestration, analysis-workflow, code-review, and directly affected skill guidance; the existing Obsidian failure-category research note and index if its title or link changes; bounded read-only analysis of canonical orchestration metrics; and, only when the audit supports it, advisory routing, routing-experiment, effort-default, telemetry, documentation, and focused test code.
- Out of scope: New policy engines, schedulers, routing experiments, telemetry fields, analytics persistence, general prompt-router redesign, model-provider additions, unrelated skills, formatting-only rewrites, live infrastructure behavior, damage-control weakening, and implementation of broader architectural enforcement candidates such as generated WSL link parity.
- Preserve: Explicit user overrides; user-requested model selection; provider availability and compatibility checks; simple `modelSize` resolution where it remains an active interface; repository and module ownership; destructive-operation, secret, wrong-target, external-protocol, live-rollout, and incident-recovery boundaries; unrelated working-tree changes; and current behavior unless this plan names it for evidence-backed simplification.
- Assumptions: Canonical local orchestration metrics can establish observed use, classifications, cost, duration, status, and structural-validation availability but may not establish causal task quality. Missing quality evidence is reported as insufficient rather than equivalence; an experiment that cannot answer its stated decision and has no external interface receives an explicit close-or-redesign disposition, but this plan does not add replacement telemetry or a new experiment. `/do-it` must create and own the implementation worktree before changing files, then archive, commit, merge with `--no-ff`, verify merged HEAD, and remove only its owned worktree and branch.

## Tasks

- [ ] **T1: Audit routing policy value with bounded evidence**
  - Files: `docs/research/obsidian-vault/agent-workflows/patterns/eliminating-failure-categories.md`, `.tmp/routing-policy-audit.ts`, `.tmp/routing-policy-audit.json`
  - Change: Read only canonical orchestration metric records from the configured metrics root with a disposable bounded script under `.tmp/`. Record exact selected file identifiers, UTC interval, reader bounds, malformed or skipped records, executable aggregate definitions, advisory policy versions, task classes, preferred/accepted/mismatch counts, topology mismatches, experiment assignments, arms, structural validation availability, completion status, cost, and duration. Do not attribute interaction-level friction or process success to one worker route or claim causal quality. Preserve the audit method, interval, bounds, aggregate definitions, totals, skipped-record counts, evidence limits, and retain/simplify/remove/evidence-insufficient dispositions in the tracked research note; the disposable script and JSON are reproducibility aids, not the durable evidence. Cover explicit model resolution, provider compatibility, size aliases, effort defaults, the task-class advisory matrix, mismatch classification, topology classification, and `codex-routing-outcomes-v1`. Evidence-insufficient cannot authorize behavioral removal or simplification; closing experiment-only machinery is allowed when the experiment cannot answer its stated decision and no active interface depends on it. Do not add analytics schemas, telemetry, a replacement experiment, or permanent audit code.
  - Done when: Re-running the disposable script reproduces the report totals from the recorded inputs and interval; the tracked note contains enough bounded method and aggregate detail to reconstruct that report without retaining `.tmp/`; the report states evidence limits and does not infer quality from status or structural validation; every active responsibility in `pi/lib/model-routing.ts` has a disposition tied to observed evidence, an external interface, or an explicit inability to decide; and the note distinguishes deterministic safety mechanisms from defaults, heuristics, and contextual judgment.
  - Verify: `bun .tmp/routing-policy-audit.ts --check .tmp/routing-policy-audit.json && rg -n "Routing policy case study|Retain|Simplify|Remove|Evidence insufficient" docs/research/obsidian-vault/agent-workflows/patterns/eliminating-failure-categories.md`

- [ ] **T2: Refactor durable guidance around values and boundaries**
  - Files: `AGENTS.md`, `pi/AGENTS.md`, `pi/skills/development-philosophy/codebase-design.md`, `pi/skills/skills-engineer/SKILL.md`, `pi/skills/orchestration/SKILL.md`, `pi/skills/analysis-workflow/SKILL.md`, `pi/skills/analysis-workflow/debugging.md`, `pi/skills/code-review/SKILL.md`, `pi/multi-team/agents/orchestrator.md`, `pi/multi-team/agents/engineering-lead.md`, `pi/multi-team/agents/planning-lead.md`, `pi/multi-team/agents/validation-lead.md`
  - Depends on: T1
  - Change: Establish the hierarchy "delete unnecessary choices; prefer direct code; enforce consequential invariants at their owner; provide overridable defaults; preserve contextual judgment; add policy machinery only after demonstrated failure; retire machinery that no longer changes outcomes." Replace the blanket deterministic-tooling and fallback prohibitions with invariant-focused language. Add value/boundary/outcome/procedure/default/heuristic classification to skill authoring, make completion evidence apply to operational workflows rather than every prose step, consolidate duplicated bound-before-work guidance, remove mutable model ladders from global policy, and express review and debugging rules through changed evidence rather than fixed counts or reasoning rituals. Keep exact procedures where order is part of correctness and preserve every named safety boundary. Prefer deletion and consolidation over adding parallel explanations.
  - Done when: The determinism/flexibility hierarchy is owned by codebase design, instruction classification by skills-engineer, delegation topology by orchestration, bounded diagnosis by analysis-workflow, and changed-evidence review limits by code-review; global instructions contain no mutable model ladder, blanket determinism mandate, blanket fallback ban, universal escalation choreography, or fixed reasoning count; operational skills retain only procedures whose sequence affects correctness; and direct review of the edited authoritative sections confirms that destructive Git, credential, wrong-target, external-protocol, live-rollout, and incident controls remain normative.
  - Verify: `git diff --check -- AGENTS.md pi/AGENTS.md pi/skills/development-philosophy/codebase-design.md pi/skills/skills-engineer/SKILL.md pi/skills/orchestration/SKILL.md pi/skills/analysis-workflow/SKILL.md pi/skills/analysis-workflow/debugging.md pi/skills/code-review/SKILL.md && ! rg -n "Use deterministic code/tooling|Do not add try-catch wrappers|Every step ends with|2-3 alternative causes|Stop after 2 failed attempts|A review is one terminal pass" AGENTS.md pi/AGENTS.md pi/skills`

- [ ] **T3: Simplify routing implementation only as the audit supports**
  - Files: Candidate targets selected only by actionable T1 dispositions: `pi/lib/model-routing.ts`, `pi/lib/orchestration-telemetry.ts`, `pi/extensions/subagent/index.ts`, `pi/extensions/fable.ts`, `pi/extensions/workflow-commands.ts`, `pi/extensions/workflow-friction-review.ts`, `pi/docs/orchestration-telemetry.md`, and their directly affected tests under `pi/tests/`
  - Depends on: T1, T2
  - Change: Apply only actionable T1 dispositions; skip executable mutation when every disposition is retain or evidence-insufficient. Preserve active user-facing model resolution and compatibility behavior. An evidence-insufficient disposition cannot authorize behavioral removal or simplification; removal requires a demonstrated absence of active interfaces and consequential behavior, except that experiment-only machinery may close when it cannot answer its stated decision. Remove advisory matrices, classifications, topology judgments, experiment assignment, effort coupling, telemetry schema fields, documentation, or tests only when the disposition supports it; retain a mechanism when evidence or an active interface requires it, while making heuristics overridable and clearly non-normative. Search `pi/extensions/subagent/legacy-adapter.ts` and `pi/extensions/tasks.ts` as preservation interfaces and change them only if their accepted or rejected model-field contracts change. Reconcile the four multi-team agent files from T2 with retained size semantics. Do not replace removed machinery, create compatibility shims for private telemetry, add another abstraction layer, or modify the analytics system. For an executable change, run `model-routing.test.ts` and `subagent.test.ts`; add `fable.test.ts`, `workflow-commands-pure.test.ts`, or `orchestration-telemetry.test.ts` plus `orchestration-stats.test.ts` only when that surface changes, and run typecheck because the shared TypeScript contract changed.
  - Done when: T3 is explicitly skipped with the T1 disposition as evidence or every retained routing mechanism maps to an active interface, safety constraint, or T1 evidence; every removed advisory or experimental path is removed through its producers, schema readers, consumers, documentation, and tests without parallel state; affected Fable defaults, commit-planning resolution, explicit overrides, legacy accepted fields, task rejection, and provider compatibility still work; and no routing mismatch is treated as failure or acceptance evidence.
  - Verify: `cd pi && pnpm run typecheck && pnpm test model-routing.test.ts subagent.test.ts` plus only the focused test files mapped above to additional changed surfaces; skip this command when T3 makes no executable change.

## Validation

- [ ] Record successful T1-T3 task checks, then run final cross-task validation: for each symbol actually removed by an actionable T1 disposition, require zero live references in its affected producer, consumer, agent-guidance, documentation, and test paths; if Markdown links changed, resolve those changed relative links to existing files with a bounded `pathlib` check; run `git diff --check` over the complete change; and directly confirm the edited authoritative sections still contain normative destructive Git, credential, wrong-target, external-protocol, live-rollout, and incident controls.

## Retention

Keep incomplete work at `.specs/flexible-agent-guidance/plan.md`. After completion, `/do-it` archives this directory to `.specs/archive/flexible-agent-guidance/`.

## Execution Status

- State: Ready; implementation has not started.
- Blocker: None.
- Next: T1.
- Resume: `/do-it .specs/flexible-agent-guidance/plan.md`
