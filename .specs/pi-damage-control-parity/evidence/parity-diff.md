# Parity Diff

Schema: Pi fixtures evaluate synthetic command/path inputs directly against the TypeScript damage-control engine. Claude subprocess oracle for every pattern was not completed in this implementation window.

- Implemented fixture families: `rm`, `rm -f`, `rm -rf`, `git rm`, `git push --force`, `git push --force-with-lease`, `docker compose down`, synthetic secret read, zero-access path read.
- Mismatch count for implemented TypeScript fixtures: 0 (see `pi-damage-control-tests-final.log`).
- Coverage debt: Claude subprocess oracle and per-pattern positive fixture coverage remain deferred; do not claim all-pattern parity.
