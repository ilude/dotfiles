# Power Calculation

N = 30
Gate: point estimate >= 0 and 95% bootstrap CI lower >= -0.05; negative_lift >= -0.05 remains a separate required gate.

This chooses B-06 option (b). Wave 1 is a scaffold intended to validate deterministic plumbing before costly fixture expansion. The loosened lower-bound gate avoids over-claiming significance from 30 paired tasks while still blocking clearly harmful retrieval behavior. Fixture expansion to N >= 100 remains the next step if the scaffold becomes the production decision gate.
