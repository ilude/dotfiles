# Wave validation evidence
Command: task acceptance greps, pnpm test damage-control.test.ts, pnpm run typecheck, docs greps
Cwd: /c/Users/mglenn/.dotfiles
## T1 runtime evidence check
Command: grep/read upstream Pi source for beforeToolCall, runner emission, block-to-nonexecution, sentinel test
- hook registration line evidence: packages/coding-agent/src/core/agent-session.ts registers agent.beforeToolCall to call extensionRunner.emitToolCall before normal tool execution.
- runner emission line evidence: packages/coding-agent/src/core/extensions/runner.ts emits tool_call handlers and returns the first { block: true } decision.
- nonexecution line evidence: packages/agent/src/agent-loop.ts awaits beforeToolCall and converts a block into an error result before invoking executeTool.
- sentinel line evidence: packages/coding-agent/test/suite/agent-session-model-extension.test.ts has a sentinelTool test where blocked tool_call does not execute the tool body.
Conclusion: normal Pi agent execution has a pre-execution tool_call boundary: AgentSession hook registration -> extension runner emission -> agent loop block result before executeTool. The upstream sentinel test verifies blocked tool_call prevents tool body execution.
## T1 ownership evidence check
Binary file .specs/pi-dc-runtime-guard/evidence/functions-bash-ownership.md matches
## Static guardrails grep (expected literal destructive strings only; no child_process/spawn/exec/bash -c)
134:				reason: "Invoke-Expression executes dynamically constructed code",
865:			"git reset --hard",
## Docs surface grep
pi/README.md:104:Pi damage-control is Pi-only and lives in `pi/extensions/damage-control.ts` plus focused sibling modules for rule loading, pure engine decisions, and opt-in debug logging. The canonical command/path policy is Claude's in-repo `claude/hooks/damage-control/patterns.yaml` when present, parsed through the full-YAML Python/PyYAML helper and normalized into Pi's TypeScript engine. `PI_DAMAGE_CONTROL_CLAUDE_POLICY_PATH` can point at an explicit Claude policy; if that override is missing or invalid, damage-control fails closed. If no override is set and the in-repo Claude policy is unavailable, Pi falls back to `pi/damage-control-rules.yaml` in explicit Pi-only mode.
pi/README.md:106:Debug logging is disabled by default. To enable redacted diagnostic logs for a short investigation, set `PI_DAMAGE_CONTROL_DEBUG=1`; logs may appear at `.pi/damage-control-debug.log` and `~/.pi/agent/damage-control-debug.log`. Do not print old debug logs directly: inventory paths first and inspect only redacted, synthetic entries.
pi/README.md:111:cd pi/tests && pnpm test damage-control.test.ts
pi/README.md:164:just safe     # damage-control only (safety rules)
pi/README.md:165:just chain    # damage-control + plan-build-review pipeline
pi/README.md:167:just full     # all extensions (damage-control + chain + subagent + quality-gates + session-hooks)
pi/README.md:176:pi -e ~/.dotfiles/pi/extensions/damage-control.ts   # explicit load
pi/README.md:185:### `damage-control.ts`
pi/README.md:187:Pi damage-control is a Pi-native adapter for the intent of the Claude Code damage-control hooks. It uses Pi extension APIs, status text, `/doctor`, and `/permissions` rather than importing the Claude hook runtime. Current coverage is intentionally bounded: Claude `bashToolPatterns` (Bash-only, excluding `exfil` entries from all-pattern parity claims) plus Claude path/write sections that map to Pi's tool surfaces. Semantic git analysis, AST bash analysis, taint/sequence detection, and post-tool secret-output detection remain deferred.
pi/README.md:195:Primary policy file: `~/.dotfiles/claude/hooks/damage-control/patterns.yaml`. Fallback Pi-only rules file: `~/.dotfiles/pi/damage-control-rules.yaml`.
pi/README.md:343:Producer wiring: `subagent` and `damage-control` write to the
pi/README.md:394:Decision provenance categories: `rule` (config-driven, what damage-control
pi/README.md:756:- Tool-call inputs and outputs as Pi received them, including truncation metadata and a `full_output_path` reference when output is spilled to disk (`tool_call`, `tool_result`).
pi/README.md:830:| `tool_call` (in `transcript-tools.ts`) | `tool_call` | Cloned + redacted parameters |
pi/README.md:871:| `~/.dotfiles/pi/damage-control-rules.yaml` | Safety rules for damage-control extension |
pi/extensions/README.md:28:`claude/hooks/damage-control/patterns.yaml` is parsed through
pi/extensions/README.md:186:Damage-control is Pi-only safety enforcement for shell and file-tool decisions. `damage-control.ts` is the Pi event adapter; `damage-control-rules.ts` loads and validates the policy schema with `pi/lib/yaml-mini.ts`; `damage-control-engine.ts` contains pure deny/ask/allow decisions; `damage-control-debug.ts` contains opt-in redacted logging.
pi/extensions/README.md:188:Damage-control covers Pi's normal runtime `tool_call` event path for registered tools such as `bash`, `pwsh`, `read`, `write`, and `edit`. It is distinct from Claude Code `PreToolUse` hooks under `claude/hooks/`, and it does not claim coverage for external/direct API developer-tool surfaces such as this harness's `functions.bash` unless that surface is explicitly routed through Pi extension hooks. When investigating a bypass, first identify the execution boundary before adding a wrapper.
pi/extensions/README.md:190:Automated destructive-command coverage must treat commands such as `rm -rf`, `git reset --hard`, and `git clean -fd` as inert string inputs to the damage-control handler. Live destructive probes are out of scope for routine tests and require a separate plan with disposable temp-dir safeguards.
pi/extensions/README.md:192:Debug logging is off by default. Set `PI_DAMAGE_CONTROL_DEBUG=1` only while investigating, then inspect `.pi/damage-control-debug.log` or `~/.pi/agent/damage-control-debug.log` for redacted synthetic entries. Do not print old damage-control debug logs because they may predate redaction guarantees.
pi/extensions/README.md:197:cd pi/tests && pnpm test damage-control.test.ts
.specs/pi-dc-runtime-guard/evidence/damage-control-test.md:2:Command: cd pi/tests && pnpm test damage-control.test.ts
.specs/pi-dc-runtime-guard/evidence/damage-control-test.md:6:> vitest run "damage-control.test.ts"
.specs/pi-dc-runtime-guard/evidence/damage-control-test.md:11: [32m✓[39m tests/damage-control.test.ts [2m([22m[2m63 tests[22m[2m)[22m[33m 2913[2mms[22m[39m
.specs/pi-dc-runtime-guard/evidence/damage-control-test.md:12:     [33m[2m✓[22m[39m registers /damage-control and /dc session-local mode commands [33m 341[2mms[22m[39m
.specs/pi-dc-runtime-guard/evidence/damage-control-test.md:24:Conclusion: damage-control handler regression tests pass; destructive command strings are inert test inputs.
Binary file .specs/pi-dc-runtime-guard/evidence/functions-bash-ownership.md matches
.specs/pi-dc-runtime-guard/evidence/preflight.md:2:Command: mkdir -p .specs/pi-dc-runtime-guard/evidence; export PI_MONO_DIR="${PI_MONO_DIR:-C:/Projects/Personal/pi-mono}"; git status --short; test -f pi/tests/damage-control.test.ts; test -d "$PI_MONO_DIR"
.specs/pi-dc-runtime-guard/evidence/preflight.md:13:damage-control.test.ts: present
.specs/pi-dc-runtime-guard/evidence/runtime-boundary.md:22:C:/Projects/Personal/pi-mono/packages/coding-agent/test/suite/agent-session-model-extension.test.ts:96:	it("allows extension tool_call handlers to block tool execution", async () => {
.specs/pi-dc-runtime-guard/evidence/runtime-boundary.md:28:- runner emission line evidence: packages/coding-agent/src/core/extensions/runner.ts emits tool_call handlers and returns the first { block: true } decision.
.specs/pi-dc-runtime-guard/evidence/runtime-boundary.md:31:811:			const handlers = ext.handlers.get("tool_call");
.specs/pi-dc-runtime-guard/evidence/runtime-boundary.md:44:- sentinel line evidence: packages/coding-agent/test/suite/agent-session-model-extension.test.ts has a sentinelTool test where blocked tool_call does not execute the tool body.
.specs/pi-dc-runtime-guard/evidence/runtime-boundary.md:45:96:	it("allows extension tool_call handlers to block tool execution", async () => {
.specs/pi-dc-runtime-guard/evidence/runtime-boundary.md:46:110:					pi.on("tool_call", async () => ({ block: true, reason: "Blocked by test" }));
.specs/pi-dc-runtime-guard/evidence/runtime-boundary.md:49:Conclusion: normal Pi agent execution has a pre-execution tool_call boundary: AgentSession hook registration -> extension runner emission -> agent loop block result before executeTool. The upstream sentinel test verifies blocked tool_call prevents tool body execution.
.specs/pi-dc-runtime-guard/evidence/wave-validation.md:2:Command: task acceptance greps, pnpm test damage-control.test.ts, pnpm run typecheck, docs greps
.specs/pi-dc-runtime-guard/evidence/wave-validation.md:7:- runner emission line evidence: packages/coding-agent/src/core/extensions/runner.ts emits tool_call handlers and returns the first { block: true } decision.
.specs/pi-dc-runtime-guard/evidence/wave-validation.md:9:- sentinel line evidence: packages/coding-agent/test/suite/agent-session-model-extension.test.ts has a sentinelTool test where blocked tool_call does not execute the tool body.
.specs/pi-dc-runtime-guard/evidence/wave-validation.md:10:Conclusion: normal Pi agent execution has a pre-execution tool_call boundary: AgentSession hook registration -> extension runner emission -> agent loop block result before executeTool. The upstream sentinel test verifies blocked tool_call prevents tool body execution.
.specs/pi-dc-runtime-guard/evidence/wave-validation.md:12:Binary file .specs/pi-dc-runtime-guard/evidence/functions-bash-ownership.md matches
.specs/pi-dc-runtime-guard/plan.md:11:Safe destructive-command probes (`rm -rf` in `/tmp`, `git reset --hard` in a disposable repo, and `git clean -fd` in a disposable repo) were run through this session's direct developer tool surface (`functions.bash`) and were not blocked. Claude Code hooks do block comparable commands because Claude Code invokes host-level `PreToolUse` hooks before tool execution.
.specs/pi-dc-runtime-guard/plan.md:13:Source inspection and web/docs research indicate normal Pi agent tool calls already have a similar pre-execution boundary: Pi wires extension `tool_call` handlers through `AgentSession.agent.beforeToolCall`, and the lower-level agent loop converts `{ block: true }` into an immediate error result before the tool body executes. The unresolved problem is therefore boundary-specific: determine whether the observed `functions.bash` bypass is outside normal Pi runtime, and prevent future work from confusing direct API/developer tools with Pi's normal LLM `bash` tool.
.specs/pi-dc-runtime-guard/plan.md:15:A prior speculative implementation tried to wrap `bash` in `pi/extensions/damage-control.ts` and was reverted. This plan requires evidence first, then either tests/docs in dotfiles or a stop condition for a separate upstream/harness plan.
.specs/pi-dc-runtime-guard/plan.md:43:| Add a `bash` tool wrapper in `pi/extensions/damage-control.ts` immediately | Places guard near execution and follows Pi examples for overriding built-ins | Can duplicate `tool_call` checks, re-prompt ask rules, and still not protect `functions.bash` if that surface bypasses Pi tools | Rejected until a failing runtime regression proves it is needed |
.specs/pi-dc-runtime-guard/plan.md:45:| Reuse upstream generic runtime-block evidence plus add dotfiles damage-control handler tests | Separates Pi core runtime proof from dotfiles damage-control policy proof | Does not patch direct API/developer `functions.bash` if that owner is outside this repo | **Selected** |
.specs/pi-dc-runtime-guard/plan.md:47:| Patch Claude hooks instead | Claude already blocks via `PreToolUse` | Does not address Pi/API harness behavior | Rejected |
.specs/pi-dc-runtime-guard/plan.md:49:For medium tasks below, the concrete rejected alternative is a broad tool-wrapper migration. It is rejected unless tests prove `tool_call` is not honored in the relevant in-scope Pi execution path.
.specs/pi-dc-runtime-guard/plan.md:55:1. Recording source evidence for Pi's normal pre-execution `tool_call` boundary, preferably including the existing upstream generic sentinel-tool test.
.specs/pi-dc-runtime-guard/plan.md:56:2. Adding or hardening dotfiles tests that prove the Pi damage-control extension blocks dangerous bash command strings through its registered `tool_call` handler.
.specs/pi-dc-runtime-guard/plan.md:57:3. Identifying whether the observed `functions.bash` bypass is owned by local source. If no local owner is found, document it as an external/direct API developer-tool surface rather than claiming it is fixed.
.specs/pi-dc-runtime-guard/plan.md:63:- **Test command**: `cd pi/tests && pnpm install --frozen-lockfile && pnpm test damage-control.test.ts` for task-specific Pi extension tests.
.specs/pi-dc-runtime-guard/plan.md:70:| Preflight | `mkdir -p .specs/pi-dc-runtime-guard/evidence; export PI_MONO_DIR="${PI_MONO_DIR:-C:/Projects/Personal/pi-mono}"; git status --short; test -f pi/tests/damage-control.test.ts; test -d "$PI_MONO_DIR" || { echo "PI_MONO_DIR missing: $PI_MONO_DIR"; exit 20; }` | none | `.specs/pi-dc-runtime-guard/evidence/preflight.md` |
.specs/pi-dc-runtime-guard/plan.md:72:| Runtime boundary research | `grep -R "beforeToolCall\|emitToolCall\|allows extension tool_call handlers to block tool execution" -n "$PI_MONO_DIR/packages/agent/src" "$PI_MONO_DIR/packages/coding-agent/src" "$PI_MONO_DIR/packages/coding-agent/test" | sed -n '1,200p'` | none | `.specs/pi-dc-runtime-guard/evidence/runtime-boundary.md` |
.specs/pi-dc-runtime-guard/plan.md:73:| Local ownership check | `grep -R "functions.bash\|developer tool\|tool adapter" -n "$PI_MONO_DIR/packages" pi 2>/dev/null | sed -n '1,120p' || true` | none | `.specs/pi-dc-runtime-guard/evidence/functions-bash-ownership.md` |
.specs/pi-dc-runtime-guard/plan.md:74:| Task-specific tests | `cd pi/tests && pnpm test damage-control.test.ts` | none | `.specs/pi-dc-runtime-guard/evidence/damage-control-test.md` |
.specs/pi-dc-runtime-guard/plan.md:76:| Repo-wide validation | `make check` or documented fallback: `make test-quick && make lint-python && cd pi/tests && pnpm test damage-control.test.ts && cd ../extensions && pnpm run typecheck` | none | `.specs/pi-dc-runtime-guard/evidence/repo-validation.md` |
.specs/pi-dc-runtime-guard/plan.md:92:- [ ] T2: Add failing/protective regression tests for Pi damage-control handler boundary
.specs/pi-dc-runtime-guard/plan.md:140:| T2 | Add failing/protective regression tests for Pi damage-control handler boundary | 1-2 | feature | medium | qa-engineer | -- |
.specs/pi-dc-runtime-guard/plan.md:152:- Description: Inspect local upstream Pi source read-only through `PI_MONO_DIR` and write `.specs/pi-dc-runtime-guard/evidence/runtime-boundary.md`. The evidence must identify: hook registration source, runner emission source, block-to-nonexecution source, and any existing upstream sentinel-tool test proving a blocked `tool_call` does not execute the tool body. If `PI_MONO_DIR` is missing, mark the task blocked with remediation; do not guess.
.specs/pi-dc-runtime-guard/plan.md:153:- Files: `.specs/pi-dc-runtime-guard/evidence/runtime-boundary.md`; optional `.specs/pi-dc-runtime-guard/evidence/functions-bash-ownership.md`.
.specs/pi-dc-runtime-guard/plan.md:159:  2. [ ] Local ownership of `functions.bash` is recorded without treating absence of literal strings as proof of ownership.
.specs/pi-dc-runtime-guard/plan.md:160:     - Verify: `test -s .specs/pi-dc-runtime-guard/evidence/functions-bash-ownership.md && grep -E "found owner|not found locally|limitation" .specs/pi-dc-runtime-guard/evidence/functions-bash-ownership.md`
.specs/pi-dc-runtime-guard/plan.md:164:**T2: Add failing/protective regression tests for Pi damage-control handler boundary** [medium] -- qa-engineer
.specs/pi-dc-runtime-guard/plan.md:165:- Description: Add dotfiles tests that exercise the registered `damage-control` `tool_call` handler for bash commands. These tests prove damage-control policy/handler behavior in this repo, while T1 records upstream runtime nonexecution evidence. Automated tests must not execute destructive commands; they must use command strings as inert input data.
.specs/pi-dc-runtime-guard/plan.md:166:- Files: `pi/tests/damage-control.test.ts` and optional `pi/tests/helpers/*`.
.specs/pi-dc-runtime-guard/plan.md:168:  1. [ ] `rm -rf`, `git reset --hard`, and `git clean -fd` are blocked through the registered bash `tool_call` handler without shell/process execution.
.specs/pi-dc-runtime-guard/plan.md:169:     - Verify: `cd pi/tests && pnpm test damage-control.test.ts`
.specs/pi-dc-runtime-guard/plan.md:173:     - Verify: `cd pi/tests && pnpm test damage-control.test.ts`
.specs/pi-dc-runtime-guard/plan.md:174:     - Pass: A `git status --short` or `pwd` case returns `undefined` from the damage-control handler in default mode.
.specs/pi-dc-runtime-guard/plan.md:177:     - Verify: `grep -n "child_process\|spawn\|exec\|bash -c\|rm -rf /\|git reset --hard" pi/tests/damage-control.test.ts || true`
.specs/pi-dc-runtime-guard/plan.md:187:  2. Run `cd pi/tests && pnpm install --frozen-lockfile && pnpm test damage-control.test.ts`.
.specs/pi-dc-runtime-guard/plan.md:189:  4. Confirm the result classifies the observed `functions.bash` bypass as one of: local owner found, not found locally, or blocked pending harness source.
.specs/pi-dc-runtime-guard/plan.md:197:  - If dotfiles damage-control handler tests fail: fix `pi/extensions/damage-control.ts` so the registered handler blocks dangerous bash command strings. Avoid wrapping `bash` unless a dotfiles test proves the existing handler path is bypassed in this repo.
.specs/pi-dc-runtime-guard/plan.md:198:  - If dotfiles tests pass and the only observed bypass is `functions.bash` with no local owner: do not patch `damage-control.ts`; add a diagnostic/doc note that direct API/developer tool surfaces are not covered by Pi extension hooks in this repo.
.specs/pi-dc-runtime-guard/plan.md:200:- Files: expected `pi/extensions/damage-control.ts`, `pi/tests/damage-control.test.ts`, `pi/README.md`, `pi/extensions/README.md`, or `.specs/pi-dc-runtime-guard/evidence/*.md`. No upstream writes.
.specs/pi-dc-runtime-guard/plan.md:203:     - Verify: `git diff -- pi/extensions/damage-control.ts pi/tests/damage-control.test.ts pi/README.md pi/extensions/README.md .specs/pi-dc-runtime-guard | sed -n '1,260p'`
.specs/pi-dc-runtime-guard/plan.md:207:     - Verify: `cd pi/tests && pnpm test damage-control.test.ts`
.specs/pi-dc-runtime-guard/plan.md:220:  1. Run `cd pi/tests && pnpm test damage-control.test.ts`.
.specs/pi-dc-runtime-guard/plan.md:230:- Description: Update docs or evidence notes so future agents know how to test Pi damage-control safely. The doc must distinguish Claude Code `PreToolUse`, Pi `tool_call`, and this session's direct API/developer tool surface. State that automated tests use dangerous commands as inert strings and live destructive probes are out of scope for this plan.
.specs/pi-dc-runtime-guard/plan.md:233:  1. [ ] Documentation states which surfaces are covered by Pi damage-control.
.specs/pi-dc-runtime-guard/plan.md:234:     - Verify: `grep -R "functions.bash\|tool_call\|PreToolUse\|damage-control" -n pi/README.md pi/extensions/README.md .specs/pi-dc-runtime-guard 2>/dev/null | sed -n '1,160p'`
.specs/pi-dc-runtime-guard/plan.md:236:     - Fail: Docs imply `functions.bash` is protected by Pi extensions without evidence.
.specs/pi-dc-runtime-guard/plan.md:248:  2. Run `cd pi/tests && pnpm test damage-control.test.ts`.
.specs/pi-dc-runtime-guard/plan.md:263:1. [ ] Pi damage-control has automated dotfiles regression coverage proving dangerous bash command strings are blocked through the registered damage-control `tool_call` handler.
.specs/pi-dc-runtime-guard/plan.md:264:   - Verify: `cd pi/tests && pnpm test damage-control.test.ts`
.specs/pi-dc-runtime-guard/plan.md:270:   - Verify: `grep -R "functions.bash\|direct API\|developer tool\|tool_call" -n pi/README.md pi/extensions/README.md .specs/pi-dc-runtime-guard 2>/dev/null | sed -n '1,160p'`
.specs/pi-dc-runtime-guard/plan.md:299:   - Command: Prefer `make check`. If full `make check` is too slow or blocked by unrelated environment prerequisites, record command, duration/error, why the failure is unrelated, then run fallback: `make test-quick && make lint-python && cd pi/tests && pnpm test damage-control.test.ts && cd ../extensions && pnpm run typecheck`.
.specs/pi-dc-runtime-guard/plan.md:339:- Do not repeat the failed pattern of editing `pi/extensions/damage-control.ts` before a failing in-scope test exists.
.specs/pi-dc-runtime-guard/plan.md:340:- Existing source evidence seen before this plan: `C:/Projects/Personal/pi-mono/packages/coding-agent/src/core/agent-session.ts` installs `agent.beforeToolCall`; `C:/Projects/Personal/pi-mono/packages/agent/src/agent-loop.ts` turns `beforeToolCall` blocks into immediate error tool results before execution; `C:/Projects/Personal/pi-mono/packages/coding-agent/src/core/extensions/runner.ts` emits `tool_call` handlers and stops on block; `C:/Projects/Personal/pi-mono/packages/coding-agent/test/suite/agent-session-model-extension.test.ts` includes a generic sentinel-tool block test.
.specs/pi-dc-runtime-guard/plan.md:341:- The observed non-blocking probes were run via this session's `functions.bash`, not via a normal Pi LLM tool call. Treat that as an external/direct API developer-tool surface unless T1 finds a local owner.
.specs/pi-dc-runtime-guard/review-1/applied-fixes.md:5:| Handler-only tests can be mistaken for runtime proof | Bug | Context & Motivation, Objective, T1, T2, Success Criteria | Require upstream existing generic runtime evidence and damage-control-specific dotfiles handler tests; stop claiming dotfiles tests alone prove AgentSession behavior | Preserve T1/T2 IDs; clarify acceptance criteria |
.specs/pi-dc-runtime-guard/review-1/applied-fixes.md:7:| Plan does not close functions.bash bypass | Bug | Objective, T3, Success Criteria, Handoff Notes | State this plan can document/diagnose external API harness bypass but cannot patch out-of-repo developer tools unless owner is found in scope | Preserve checklist; no new task |
.specs/pi-dc-runtime-guard/review-1/product-manager.md:5:evidence: The plan’s objective is to explain why `functions.bash` bypassed protection, but Wave 1–3 mostly test the existing Pi `tool_call` handler. `pi/extensions/damage-control.ts` already registers `pi.on("tool_call")` for `bash`, and `pi/tests/damage-control.test.ts` already has extensive evaluator coverage. If the bypass is outside Pi, these tests cannot close the user-visible gap.
.specs/pi-dc-runtime-guard/review-1/product-manager.md:6:required_fix: Reframe success around the actual exposed surface: either identify the owner of `functions.bash` and file/patch that layer, or explicitly reduce scope to a documentation-only boundary note. Do not present Pi hook tests as fixing API-tool bypass reliability.
.specs/pi-dc-runtime-guard/review-1/product-manager.md:10:evidence: The task breakdown uses six handoffs across three waves plus five final gates for a likely 1–2 file outcome. The selected path even says “If Pi `tool_call` regression passes…do not patch,” which could make most of Wave 2/3 process overhead for a doc note.
.specs/pi-dc-runtime-guard/review-1/qa-engineer.md:6:   **evidence:** Objective #1 requires proving normal Pi agent-loop calls are blocked before execution, but T2 acceptance only requires invoking the registered `tool_call` handler and asserting `{ block: true }`. That still allows the exact false positive: helper/handler returns block while the runtime ignores it and executes anyway.  
.specs/pi-dc-runtime-guard/review-1/qa-engineer.md:7:   **required_fix:** Add a required integration-style test with a fake tool executor/sentinel side effect and assert the executor is not called when `beforeToolCall`/`tool_call` returns block.
.specs/pi-dc-runtime-guard/review-1/qa-engineer.md:14:   **evidence:** T2 AC1 requires `rm -rf`, `git reset --hard`, and `git clean -fd` be blocked “without executing shell commands,” but the verify command is just `pnpm test damage-control.test.ts`; there is no required spy/mock proving no shell/process execution occurred.  
.specs/pi-dc-runtime-guard/review-1/reviewer.md:10:  evidence: "T2 asks for tests that exercise the registered damage-control `tool_call` handler, but allows “a local minimal mock” if practical. A mock can assert handler return values without proving the Pi agent-loop prevents tool execution, while Success Criterion 1 claims a pre-execution boundary regression."
.specs/pi-dc-runtime-guard/review-1/security-reviewer.md:23:  required_fix: "Add an acceptance criterion that test code uses pure handler invocation or a mocked tool executor, plus a grep/static check for child_process/spawn/exec and shell invocations in `pi/tests/damage-control.test.ts`."
.specs/pi-dc-runtime-guard/review-1/synthesis.md:32:- High: plan risked testing existing Pi `tool_call` behavior while not closing the actual `functions.bash` bypass.
.specs/pi-dc-runtime-guard/review-1/synthesis.md:39:- High: upstream already has a generic `tool_call` block test; the plan should reuse that evidence or add a damage-control-specific upstream test in the right package.
.specs/pi-dc-runtime-guard/review-1/synthesis.md:40:- Medium: `functions.bash` absence from grep is not proof of out-of-scope ownership.
.specs/pi-dc-runtime-guard/review-1/synthesis.md:60:3. The plan does not actually fix or clearly scope the observed `functions.bash` bypass; Pi hook tests alone can be misreported as closing that user-visible gap.
.specs/pi-dc-runtime-guard/review-1/synthesis.md:73:2. Use existing upstream generic `tool_call` block test as boundary evidence; keep dotfiles tests focused on damage-control rules/handler behavior.
.specs/pi-dc-runtime-guard/review-1/synthesis.md:88:1. Confirmed upstream generic runtime block test exists: `C:/Projects/Personal/pi-mono/packages/coding-agent/test/suite/agent-session-model-extension.test.ts:96` defines `allows extension tool_call handlers to block tool execution`, with a tool body throwing if executed and a `tool_call` handler returning `{ block: true }`.
.specs/pi-dc-runtime-guard/review-1/typescript-pro.md:3:evidence: The plan’s objective says to prove “normal Pi agent-loop tool calls are blocked before execution”, but T2 only targets `pi/tests/damage-control.test.ts` and allows “a local minimal mock”. The actual pre-execution boundary is in upstream `packages/agent/src/agent-loop.ts:571-582` and `packages/coding-agent/src/core/agent-session.ts:379-390`, not in dotfiles’ mocked `pi.on` registration.
.specs/pi-dc-runtime-guard/review-1/typescript-pro.md:8:evidence: Upstream already has `packages/coding-agent/test/suite/agent-session-model-extension.test.ts:96` proving a generic extension `tool_call` block prevents tool execution. The plan still routes proof to dotfiles `pi/tests`, so a new test can pass while only proving `damage-control.ts` returns `{ block: true }`, not that Pi executes that decision before `bash`.
.specs/pi-dc-runtime-guard/review-1/typescript-pro.md:9:required_fix: Add a damage-control-specific upstream harness test with an executable sentinel tool that fails if called, or reuse the existing upstream test as evidence and narrow dotfiles tests to rule coverage.
.specs/pi-dc-runtime-guard/review-1/typescript-pro.md:18:evidence: T1’s `grep -R "functions.bash|developer tool|tool adapter"` is treated as ownership proof. Absence of those strings in `pi-mono` or dotfiles does not prove the API/developer tool surface is out-of-scope; it only proves those literal labels are absent.
## Docs safe policy grep
pi/README.md:116:For live smoke tests, restart/reload Pi so extension modules and policy files reload, then use a disposable temp repo with synthetic sentinel files or temporary test-only rules. Never execute shell reads against real `.env`, SSH keys, `*.pem`, or `*.key` files. On Windows/macOS, Linux-only ask rules such as `docker compose down` are best validated with deterministic Vitest tests or a temporary non-destructive ask rule.
pi/README.md:191:- **Dangerous commands** -- blocks `rm -rf`, `git reset --hard`, `dd if=`, etc.
pi/README.md:644:retrieval index/cache are derived, disposable views used by `read_expertise` so agents recall
pi/extensions/README.md:190:Automated destructive-command coverage must treat commands such as `rm -rf`, `git reset --hard`, and `git clean -fd` as inert string inputs to the damage-control handler. Live destructive probes are out of scope for routine tests and require a separate plan with disposable temp-dir safeguards.
pi/extensions/README.md:202:Safe live smoke tests must use a disposable temp repo, synthetic sentinel `.env`/key-like paths, or temporary test-only rules. Do not execute shell reads against real `.env`, SSH private keys, `*.pem`, or `*.key` files. If runtime source is not the same symlink/inode/checksum as this repo, rerun the dotfiles link/install flow or otherwise sync before live smoke. Linux-only ask rules such as `docker compose down` should be validated with unit tests on Windows/macOS unless a temporary non-destructive ask rule is used.
Binary file .specs/pi-dc-runtime-guard/evidence/functions-bash-ownership.md matches
.specs/pi-dc-runtime-guard/evidence/wave-validation.md:15:865:			"git reset --hard",
.specs/pi-dc-runtime-guard/evidence/wave-validation.md:35:pi/extensions/README.md:190:Automated destructive-command coverage must treat commands such as `rm -rf`, `git reset --hard`, and `git clean -fd` as inert string inputs to the damage-control handler. Live destructive probes are out of scope for routine tests and require a separate plan with disposable temp-dir safeguards.
.specs/pi-dc-runtime-guard/evidence/wave-validation.md:58:.specs/pi-dc-runtime-guard/plan.md:11:Safe destructive-command probes (`rm -rf` in `/tmp`, `git reset --hard` in a disposable repo, and `git clean -fd` in a disposable repo) were run through this session's direct developer tool surface (`functions.bash`) and were not blocked. Claude Code hooks do block comparable commands because Claude Code invokes host-level `PreToolUse` hooks before tool execution.
.specs/pi-dc-runtime-guard/evidence/wave-validation.md:83:.specs/pi-dc-runtime-guard/plan.md:168:  1. [ ] `rm -rf`, `git reset --hard`, and `git clean -fd` are blocked through the registered bash `tool_call` handler without shell/process execution.
.specs/pi-dc-runtime-guard/evidence/wave-validation.md:87:.specs/pi-dc-runtime-guard/plan.md:177:     - Verify: `grep -n "child_process\|spawn\|exec\|bash -c\|rm -rf /\|git reset --hard" pi/tests/damage-control.test.ts || true`
.specs/pi-dc-runtime-guard/evidence/wave-validation.md:96:.specs/pi-dc-runtime-guard/plan.md:230:- Description: Update docs or evidence notes so future agents know how to test Pi damage-control safely. The doc must distinguish Claude Code `PreToolUse`, Pi `tool_call`, and this session's direct API/developer tool surface. State that automated tests use dangerous commands as inert strings and live destructive probes are out of scope for this plan.
.specs/pi-dc-runtime-guard/evidence/wave-validation.md:115:.specs/pi-dc-runtime-guard/review-1/qa-engineer.md:14:   **evidence:** T2 AC1 requires `rm -rf`, `git reset --hard`, and `git clean -fd` be blocked “without executing shell commands,” but the verify command is just `pnpm test damage-control.test.ts`; there is no required spy/mock proving no shell/process execution occurred.  
.specs/pi-dc-runtime-guard/plan.md:11:Safe destructive-command probes (`rm -rf` in `/tmp`, `git reset --hard` in a disposable repo, and `git clean -fd` in a disposable repo) were run through this session's direct developer tool surface (`functions.bash`) and were not blocked. Claude Code hooks do block comparable commands because Claude Code invokes host-level `PreToolUse` hooks before tool execution.
.specs/pi-dc-runtime-guard/plan.md:25:- Automated tests must treat destructive commands as inert strings. Do not execute `rm -rf`, `git reset --hard`, or `git clean -fd` in automated tests.
.specs/pi-dc-runtime-guard/plan.md:26:- Live destructive probes are prohibited by this plan. If a future diagnostic requires them, create a separate plan with disposable temp-dir safeguards.
.specs/pi-dc-runtime-guard/plan.md:37:- **Decision reason:** The in-scope work is local, reversible, and validated with automated tests using destructive commands only as inert strings. No production/shared systems, paid resources, secrets, or irreversible external side effects are involved.
.specs/pi-dc-runtime-guard/plan.md:168:  1. [ ] `rm -rf`, `git reset --hard`, and `git clean -fd` are blocked through the registered bash `tool_call` handler without shell/process execution.
.specs/pi-dc-runtime-guard/plan.md:177:     - Verify: `grep -n "child_process\|spawn\|exec\|bash -c\|rm -rf /\|git reset --hard" pi/tests/damage-control.test.ts || true`
.specs/pi-dc-runtime-guard/plan.md:230:- Description: Update docs or evidence notes so future agents know how to test Pi damage-control safely. The doc must distinguish Claude Code `PreToolUse`, Pi `tool_call`, and this session's direct API/developer tool surface. State that automated tests use dangerous commands as inert strings and live destructive probes are out of scope for this plan.
.specs/pi-dc-runtime-guard/plan.md:238:     - Verify: `grep -R "inert string\|live destructive probes are out of scope\|disposable\|rm -rf\|git reset --hard\|git clean" -n pi/README.md pi/extensions/README.md .specs/pi-dc-runtime-guard 2>/dev/null | sed -n '1,160p'`
.specs/pi-dc-runtime-guard/plan.md:239:     - Pass: Documentation says automated tests use inert strings and live destructive probes are prohibited or require a separate plan.
.specs/pi-dc-runtime-guard/plan.md:265:   - Pass: Tests for `rm -rf`, `git reset --hard`, and `git clean -fd` pass without executing destructive shell operations.
.specs/pi-dc-runtime-guard/review-1/qa-engineer.md:14:   **evidence:** T2 AC1 requires `rm -rf`, `git reset --hard`, and `git clean -fd` be blocked “without executing shell commands,” but the verify command is just `pnpm test damage-control.test.ts`; there is no required spy/mock proving no shell/process execution occurred.  
.specs/pi-dc-runtime-guard/review-1/qa-engineer.md:22:   **evidence:** The plan forbids destructive commands outside temp repos, but does not forbid live execution inside temp repos during automated tests. That leaves room for tests that actually run `rm -rf`/`git reset --hard` and later claim safety because the target was disposable.  
.specs/pi-dc-runtime-guard/review-1/qa-engineer.md:23:   **required_fix:** State that automated regression tests must treat destructive commands as inert input data; any live probe must be a separate manual/diagnostic step with disposable fixtures and explicit evidence boundaries.
.specs/pi-dc-runtime-guard/review-1/reviewer.md:22:  evidence: "The plan says dangerous probes are safe because they are command strings, but T1/T4 still discuss live probes and disposable repos without a concrete safe wrapper. Commands like `rm -rf` and `git clean -fd` are easy to mis-run from the working tree."
.specs/pi-dc-runtime-guard/review-1/security-reviewer.md:22:  evidence: "The plan says not to run destructive commands outside temp dirs, but validation relies on reviewer/executor discipline. T2 only says tests must not spawn real commands; no acceptance check asserts mocks/stubs or scans for `exec`, `spawn`, `bash`, `rm -rf` execution in tests."
Exit code: 0
Conclusion: wave validation checks passed.
