High | T2 prefers `coredns.host.name={{ infisical_domain }}` on the Caddy container, but Success Criteria requires resolving to `192.168.16.241`; Docker-label DNS may publish the container/bridge IP instead of host IP. | Specify the exact Joyride label/override needed to publish the menos host IP, or make the static host entry the required path unless Caddy uses host networking.
High | T2 allows rendering the Cloudflare token into `infisical.env`; that risks exposing DNS credentials to the Infisical app container if shared. | Require a dedicated Caddy-only env file mounted/loaded only by the Caddy service, mode `0600`, with Ansible `no_log: true` on all token-handling tasks.
High | Secure logging is only referenced generally. | Add explicit acceptance criteria that Ansible tasks/templates involving `vault_infisical_cloudflare_api_token` use `no_log: true` and validation proves token is absent from rendered logs/evidence.
Medium | T2 says build with `xcaddy` and a pinned runtime, but does not require pinning the builder image, Caddy version, or plugin version. | Require pinned builder/runtime Caddy versions and a pinned `github.com/caddy-dns/cloudflare` version or commit.
Medium | Validation uses curl through default resolver, not necessarily Joyride. | Add validation proving client resolver path uses Joyride, or use `curl --resolve infisical.ilude.com:443:192.168.16.241` for deterministic TLS validation.
