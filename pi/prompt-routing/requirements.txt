# Generated/export-only compatibility file.
#
# Source of truth for this uv project is pyproject.toml plus the tracked
# uv.lock. Do not edit this file to add dependencies; update pyproject.toml
# with `uv add --project pi/prompt-routing ...` and refresh the lock instead.
# If a requirements file is needed by an external tool, regenerate it with:
#   uv export --project pi/prompt-routing --format requirements-txt --output-file pi/prompt-routing/requirements.txt

joblib>=1.4,<2.0
lightgbm>=4.0,<5.0
numpy>=2.0,<3.0
scikit-learn==1.8.0
sentence-transformers>=5.0,<6.0
