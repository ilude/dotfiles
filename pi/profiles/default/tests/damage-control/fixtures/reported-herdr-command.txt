scratch=$(mktemp -d); export APPDATA="$scratch/roaming" LOCALAPPDATA="$scratch/local" HERDR_CONFIG_PATH="$scratch/config.toml"; mkdir -p "$APPDATA" "$LOCALAPPDATA"; printf '[ui.sound]\nenabled = false\n' > "$HERDR_CONFIG_PATH"; name="focus-context-$$"; herdr --session "$name" server >/tmp/herdr-focus-$PID.log 2>&1 & p=$!; sleep 2; a=$(herdr --session "$name" workspace create --cwd "$scratch" --no-focus); b=$(herdr --session "$name" workspace create --cwd "$scratch" --focus); export HERDR_ENV=1 HERDR_SOCKET_PATH=bogus HERDR_WORKSPACE_ID=w1 HERDR_TAB_ID=w1:t1 HERDR_PANE_ID=w1:p1; echo before; herdr --session "$name" pane current; herdr --session "$name" workspace focus w2; echo after-workspace; herdr --session "$name" pane current; herdr --session "$name" workspace list; herdr --session "$name" tab focus w2:t1; echo after-tab; herdr --session "$name" pane current; herdr --session "$name" workspace list; herdr --session "$name" server stop >/dev/null || true; kill "$p" 2>/dev/null || true; rm -rf "$scratch" /tmp/herdr-focus-$PID.log
