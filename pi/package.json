{
  "name": "pi",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p extensions",
    "biome:check": "biome lint extensions lib tests",
    "test": "vitest run --config tests/vitest.config.ts",
    "test:fast": "vitest run --config tests/vitest.fast.config.ts",
    "test:integration": "vitest run --config tests/vitest.integration.config.ts",
    "test:commit-mutation": "vitest run --config tests/vitest.config.ts tests/commit-mutation.test.ts",
    "test:watch": "vitest --config tests/vitest.config.ts",
    "test:coverage": "vitest run --coverage --config tests/vitest.config.ts"
  },
  "dependencies": {
    "@aws/bedrock-token-generator": "1.1.0",
    "@duckdb/node-api": "1.5.5-r.4",
    "@mozilla/readability": "^0.5.0",
    "croner": "10.0.1",
    "jsdom": "^25.0.1",
    "proper-lockfile": "^4.1.2",
    "tree-sitter-bash": "^0.25.1",
    "turndown": "^7.2.4",
    "turndown-plugin-gfm": "^1.0.2",
    "web-tree-sitter": "^0.26.9"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.3",
    "@types/node": "^25.9.4",
    "@types/proper-lockfile": "^4.1.4",
    "@vitest/coverage-v8": "^4.1.9",
    "typescript": "^5.9.3",
    "vitest": "^4.1.9"
  }
}
